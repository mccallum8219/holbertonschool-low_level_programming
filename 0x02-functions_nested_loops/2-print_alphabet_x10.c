#include "main.h"
/**
 * print_alphabet - prints from a-z 10x in lowercase
 *
 * Return: 0
 */
void print_alphabet(void)
{
  char c, int x;
	x = 0;
	  {
	   while (co < 10)
	     }
	for (c = 'a'; c <= 'z'; c++)
	{
		_putchar(c);
	}
	x++;
	_putchar('\n');
}
